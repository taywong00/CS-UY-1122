# Assignment 2
# Name: Taylor Wong
# Net ID: tw1835

# Your code goes here
# Sorting algorithm implemented: Insertion Sort

def insertion_sort(sequence):
    # parse over items
    for partition in range(1, len(seq)): # the first elem is already sorted
        for next_item in range(partition, 0, -1): # insert the next item into its sorted place behind the partition
            if sequence[j] < sequence[j-1]: # swap the items behind partition to sort
                sequence[j-1]
                sequence[j] = seq[j]
                sequence[j-1]
            else:
                break
